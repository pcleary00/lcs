package constants


object Messages {

  val DUPLICATE_MESSAGE = "Cannot have duplicate entries in a set when requesting the longest common substrings.  Make sure that all strings are unique."
  val EMPTY_SET_MESSAGE = "Cannot have an empty set when requesting the longest common substrings.  Make sure that at least one string is provided."
}
