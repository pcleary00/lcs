package constants


object Messages {

  val DUPLICATE_MESSAGE = "Cannot have duplicate entries in a set when requesting the longest common substring"
  val EMPTY_SET_MESSAGE = "Cannot have an empty set when requesting the longest common substring"
}
